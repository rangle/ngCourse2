import Card from '../card/card';
import CardTitle from '../card/card-title';
import CardActions from '../card/card-actions';

export { Card, CardTitle, CardActions };
export const CARD_COMPONENTS = [Card, CardTitle, CardActions]
