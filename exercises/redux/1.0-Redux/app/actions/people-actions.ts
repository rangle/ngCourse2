export const SKILL_UPDATED = 'SKILL_UPDATED';

export function updateSkill(person, skillName, skillLevel) {
// Return a plain JSON object that will have the appropiate
// type, and payload to be handled by the people-reducer
  return {

  }
};

export default { updateSkill }
