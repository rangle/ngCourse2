import {Component} from '@angular/core';
const SUMMARY = require('!raw!../../assets/summary.svg');

@Component({
  selector: 'ngc-icon-summary',
  template: SUMMARY
})
export default class SummaryIcon {
}
